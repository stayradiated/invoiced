.meta
  .id.number= id
  .created= 'Created: ' + moment(created).format('Do MMMM YYYY')
  .paid-status Not Paid
.customer
  h3= customer
  h5= site
  .email= email
  .date= moment(date).format('Do MMMM YYYY')
  .price.number= numeral(cost).format('$0,0.00')
